let quote2: string = "A person who never made a mistake never tried anything new.";
let author2: string = "Albert Einstein";
let message:string = `${author2} once said, "${quote2}"`;
console.log(message);